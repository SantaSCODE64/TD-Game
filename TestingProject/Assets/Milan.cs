using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Milan : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Nazdar Milan");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
